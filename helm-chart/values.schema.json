{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "dynamic-zones-api values",
  "type": "object",
  "additionalProperties": false,
  "required": ["dynamicZonesAPI", "auth", "powerDNS", "ingress"],
  "properties": {
    "global": {
      "type": "object",
      "description": "Injected by Helm into every subchart. Must be tolerated here, or this chart cannot be used as a dependency at all, because additionalProperties is false."
    },
    "enabled": {
      "type": "boolean",
      "description": "Dependency condition of the cloud-self-service umbrella chart, which passes it down into this namespace — so it has to be allowed here for the same reason global does. A standalone install ignores it."
    },
    "nameOverride": { "type": "string" },
    "fullnameOverride": {
      "type": "string",
      "description": "Pins the resource base name. The umbrella chart sets this so adopting these releases does not rename every object."
    },
    "dynamicZonesAPI": {
      "type": "object",
      "additionalProperties": false,
      "required": ["image", "version", "hostname", "replicas", "database", "dnsPolicy"],
      "properties": {
        "image": { "type": "string", "minLength": 1 },
        "imagePullPolicy": { "type": "string", "enum": ["Always", "IfNotPresent", "Never"] },
        "version": { "type": "string", "description": "Image tag. Empty falls back to the chart appVersion, which is how a pinned chart version pins the image." },
        "hostname": { "type": "string", "minLength": 1 },
        "replicas": { "type": "integer", "minimum": 0 },
        "existingSecret": { "type": "string" },
        "apiMode": { "type": "string", "enum": ["production", "development"] },
        "corsAllowedOrigins": {
          "type": "array",
          "items": { "type": "string", "pattern": "^https?://[^/]+$" }
        },
        "initialDataProviderScript": { "type": "string" },
        "apiTokenTtlHours": { "type": "integer", "minimum": 1 },
        "apiTokenMaxTtlHours": { "type": "integer", "minimum": 1 },
        "apiTokenAllowNeverExpires": { "type": "boolean" },
        "apiBindString": { "type": "string" },
        "apiBaseUrl": { "type": "string" },
        "upstreamDNS": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "server": { "type": "string" },
            "port": { "type": "integer" },
            "tsigName": { "type": "string" },
            "tsigAlgorithm": { "type": "string" },
            "tsigSecret": { "type": "string" },
            "zone": { "type": "string" },
            "name": { "type": "string" },
            "ttl": { "type": "integer" },
            "updateInterval": { "type": "integer" }
          }
        },
        "database": {
          "type": "object",
          "additionalProperties": false,
          "required": ["type"],
          "properties": {
            "type": { "type": "string", "enum": ["postgres", "memory"] },
            "name": { "type": "string" },
            "username": { "type": "string" },
            "password": { "type": "string" },
            "hostname": { "type": "string" },
            "connectionString": { "type": "string" }
          }
        },
        "dnsPolicy": {
          "type": "object",
          "additionalProperties": false,
          "required": ["superAdminEmails"],
          "properties": {
            "superAdminEmails": {
              "type": "array",
              "items": { "type": "string", "format": "email" }
            }
          }
        },
        "zoneDefaults": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "defaultAdminTsigKeyName": { "type": "string" },
            "defaultAdminTsigKey": { "type": "string" },
            "defaultAdminTsigAlg": { "type": "string" },
            "defaultRecords": { "type": "string" },
            "defaultRecordsSoa": { "type": "string" }
          }
        }
      }
    },
    "selfServiceUI": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "hostname": { "type": "string" }
      }
    },
    "auth": {
      "type": "object",
      "additionalProperties": false,
      "required": ["oidc"],
      "properties": {
        "provider": { "type": "string" },
        "oidc": {
          "type": "object",
          "additionalProperties": false,
          "required": ["issuerUrl", "clientId"],
          "properties": {
            "issuerUrl": { "type": "string", "minLength": 1 },
            "clientId": { "type": "string", "minLength": 1 }
          }
        }
      }
    },
    "powerDNS": {
      "type": "object",
      "additionalProperties": false,
      "required": ["apiKey", "apiUrl"],
      "properties": {
        "apiKey": { "type": "string" },
        "apiUrl": { "type": "string" },
        "vhost": { "type": "string" },
        "logLevel": { "type": "integer" },
        "server": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "ip": { "type": "string" },
            "port": { "type": "integer" },
            "advertisedNameserver": { "type": "string" },
            "queryTarget": { "type": "string" }
          }
        }
      }
    },
    "ingress": {
      "type": "object",
      "additionalProperties": false,
      "required": ["enabled"],
      "properties": {
        "enabled": { "type": "boolean" },
        "className": { "type": "string" },
        "annotations": {
          "type": "object",
          "additionalProperties": { "type": "string" }
        }
      }
    }
  }
}
